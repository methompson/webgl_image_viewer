THings to do:

* Set up test server to confirm things like PWA functionality
* Install the image converter package and take the blob and compress and resize the image
* Get an nginx configuration set up correctly for serving the project
* Determine if I should port the project over to a UI manager
* Develop a docker container to serve the project
* SSL certs?
